# ============================================================
# HL CopyTrade - 环境变量配置模板（复制为 .env 后填写）
# 使用方法: cp .env.example .env  然后编辑 .env 填入你的真实信息
# ============================================================

# ------------------------------------------------------------------
# 【必填】被跟单对象（Leader / Leader）的 Hyperliquid 主钱包地址
# 程序会持续监控该地址的仓位变化，并同步到你的账户
# ------------------------------------------------------------------
HL_LEADER_ADDR=0xYOUR_LEADER_WALLET_ADDRESS

# ------------------------------------------------------------------
# 【必填】你的 Hyperliquid 主钱包地址（Follower / 跟单账户）
# 这是你存放资金、产生真实仓位的账户
# ------------------------------------------------------------------
HL_USER_MAIN_ADDR=0xYOUR_MAIN_WALLET_ADDRESS

# ------------------------------------------------------------------
# 【必填】你的 API 钱包地址（Agent Wallet）
# 在 Hyperliquid 官网 → API 中创建，专门用于程序下单（注意不是主钱包私钥）
# ------------------------------------------------------------------
HL_USER_API_ADDR=0xYOUR_API_WALLET_ADDRESS

# ------------------------------------------------------------------
# 【必填】API 钱包私钥（Agent Wallet Private Key）
# 在创建 API 钱包时获得，格式为 66 位十六进制（0x开头）
# ⚠️ 此文件是你的最高机密！绝不能外泄，更不能提交到 Git 仓库
# ------------------------------------------------------------------
HL_API_PK=0xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

# ------------------------------------------------------------------
# 【可选】飞书群机器人 Webhook，用于接收交易/告警通知
# 不需要通知时，留空即可
# ------------------------------------------------------------------
HL_FEISHU_WEBHOOK=

# ------------------------------------------------------------------
# 【可选】程序根目录（绝大多数情况下无需设置，程序会自动识别）
# HL_BASE_DIR=/home/user/hl-copytrade
# ------------------------------------------------------------------
            